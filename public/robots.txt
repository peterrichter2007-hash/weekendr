# Maison Voyage — robots.txt
# Crawl everything except /api/* (Vercel serverless routes — today
# just a health-check endpoint; not public content).
#
# OPERATOR: replace `weekendr.vercel.app` with the final domain
# (also in sitemap.xml and the canonical/og:url tags in index.html).

User-agent: *
Allow: /
Disallow: /api/

Sitemap: https://weekendr.vercel.app/sitemap.xml
